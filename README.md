# React Quiz App

If you want to run the program with no issues, just download the repository and follow the instructions:

1. On the root folder run `npm install`, then run `npm run server`
2. Click on the next link and enjoy: https://thereactquizzapp.netlify.app/
